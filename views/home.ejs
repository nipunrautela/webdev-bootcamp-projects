<%- include("partials/header.ejs") -%>

  <h1>Home</h1>
  <p><%= startingContent %></p>

  <% posts.forEach(function(post) { %>
    <h2>
      <%= post.title %>
    </h2>
    <p>
      <%= post.content %>
    </p>
  <% }); %>

<%- include("partials/footer.ejs") -%>
